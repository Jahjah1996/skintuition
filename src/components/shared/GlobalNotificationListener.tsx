/**
 * GlobalNotificationListener
 * Placeholder — consultation messaging has been removed.
 * Retained as a no-op so App.tsx import doesn't break.
 */
export function GlobalNotificationListener() {
  return null;
}
